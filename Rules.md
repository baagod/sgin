# AI 开发准则

## 核心准则

1. **读取先行 (Read-First)**：修改前 **必须** 重新读取文件最新内容，严禁依赖历史记忆或上下文缓存，确保基于文件实时状态操作。
2. **本地优先 (Local-Is-Truth)**：用户本地文件的当前内容是最高真理。严禁擅自回滚、覆盖或 “修复” 用户在对话间隙产生的手动修改。
3. **最小变更 (Minimal-Scope)**：仅修改目标逻辑，严禁改动无关代码。对于用户特有的代码风格或逻辑，除非报错，否则原样保留。
